# Mounted pages (no rebuild)

This folder is mounted to `/usr/share/nginx/html/pages` in the container.

Edit files here (e.g. `tradingview-btc.html`) then reload the page: no rebuild needed.

URLs: `/pages/tradingview-btc.html`, etc.
